MainActivity
```kotlin
class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContent {
            StarbucksAppTheme {
                // A surface container using the 'background' color from the theme
                Surface(
                    modifier = Modifier.fillMaxSize(),
                    color = Color.White
                ) {
                    val menuList = listOf(
                        Menu(
                            menuK = "아이스 카페 아메리카노",
                            menuEn = "Iced Caffe Americano",
                            price = "4,500원",
                            picture = R.drawable.americano,
                            tag = Tag.BEST
                        ),
                        Menu(
                            menuK = "아이스 카페 라떼",
                            menuEn = "Iced Caffe Latte",
                            price = "5,000원",
                            picture = R.drawable.cafelatte,
                            tag = Tag.BEST
                        ),
                        Menu(
                            menuK = "씨솔트 카라멜 콜드 브루",
                            menuEn = "Sea Salt Caramel Cold Brew",
                            price = "6,300원",
                            picture = R.drawable.caramelcoldbrew,
                            tag = Tag.NEW
                        ),
                        Menu(
                            menuK = "아이스 자몽 허니 블랙 티",
                            menuEn = "Iced Grapefruit Honey Black Tea",
                            price = "5,700원",
                            picture = R.drawable.honeytea,
                            tag = Tag.BEST
                        ),
                        Menu(
                            menuK = "콜드 브루",
                            menuEn = "Cold Brew",
                            price = "6,900원",
                            picture = R.drawable.coldbrew,
                            tag = Tag.NEW
                        )
                    )

                    Column {
                        Row(
                            horizontalArrangement = Arrangement.SpaceBetween,
                            modifier = Modifier
                                .height(50.dp)
                                .fillMaxWidth()
                        ) {
                            Button(
                                onClick = { /*TODO*/ },
                                colors = ButtonDefaults.buttonColors(Color(0xffffffff))
                            ) {
                                Text(
                                    text = "〈",
                                    fontWeight = FontWeight.Bold,
                                    color = Color(0xff404040)
                                )
                            }
                            Text(
                                text = "추천",
                                color = Color(0xff191919),
                                fontWeight = FontWeight.Bold,
                                textAlign = TextAlign.Center,
                                modifier = Modifier.padding(top = 12.dp)
                            )
                            Button(
                                onClick = { /*TODO*/ },
                                colors = ButtonDefaults.buttonColors(Color(0xffffffff))
                            ) {
                                Icon(
                                    painterResource(id = R.drawable.search),
                                    contentDescription = "SearchIcon",
                                    tint = Color(0xff404040),
                                    modifier = Modifier.size(20.dp)
                                )
                            }
                        }
                        Box(
                            modifier = Modifier
                                .height(4.dp)
                                .fillMaxWidth()
                                .background(
                                    brush = gra(
                                        "v",
                                        "shadow"
                                    ) as Brush,
                                    shape = RectangleShape
                                )
                        )
                        Column(
                            modifier = Modifier.verticalScroll(rememberScrollState())
                        ) {
                            for (index in 0 until menuList.size) {
                                AllButton(menuList[index], index)
                            }
                        }
                    }
                }
            }
        }
    }

}

fun gra(select: String, color: String): Any {
    val shadow = listOf(
        Color(0xff595959),
        Color(0xffa6a6a6),
        Color(0xffbfbfbf),
        Color(0xffd9d9d9),
        Color(0xfff2f2f2)
    )
    val shadowReverse = shadow.reversed()

    return when (select) {
        "v" -> {
            when (color) {
                "shadow" ->
                    Brush.verticalGradient(
                        shadow
                    )

                "shadowReverse" ->
                    Brush.verticalGradient(
                        shadowReverse
                    )

                else -> {}
            }
        }

        "h" -> {
            when (color) {
                "shadow" ->
                    Brush.horizontalGradient(
                        shadow
                    )

                "shadowReverse" ->
                    Brush.horizontalGradient(
                        shadowReverse
                    )

                else -> {}
            }
        }
        "R" -> {
            when (color) {
                "shadow" ->
                    Brush.radialGradient(
                        shadow
                    )

                "shadowReverse" ->
                    Brush.radialGradient(
                        shadowReverse
                    )

                else -> {}
            }
        }
        else -> {}
    }
}

data class Menu(
    val menuK: String,
    val menuEn: String,
    val price: String,
    val picture: Int,
    val tag: Tag = Tag.NONE,
): Serializable

enum class Tag {
    NONE, BEST, NEW
}


@Composable
fun AllButton(menuList: Menu, index: Int) {
    val context = LocalContext.current

    Spacer(modifier = Modifier.height(5.dp))

    Button(
        onClick = {
            val intent = Intent(context, MenuInfoActivity::class.java)
            intent.putExtra("menu", menuList)
            intent.putExtra("currentIndex", index)
            context.startActivity(intent)
        },
        modifier = Modifier
            .height(145.dp)
            .width(388.dp),
        shape = RoundedCornerShape(10.dp),
        colors = ButtonDefaults.buttonColors(Color(0xffffffff)),
        contentPadding = PaddingValues(0.dp)
    ) {
        Row(
            horizontalArrangement = Arrangement.Start,
            verticalAlignment = Alignment.CenterVertically,
            modifier = Modifier
                .height(145.dp)
                .width(388.dp)
                .padding(start = 30.dp)
        ) {
            Surface(
                shape = CircleShape,
                modifier = Modifier
                    .size(100.dp)
                    .padding(0.dp)
            ) {
                Image(
                    painter = painterResource(id = menuList.picture),
                    contentDescription = "",
                    contentScale = ContentScale.Crop
                )
            }
            Spacer(modifier = Modifier.width(20.dp))
            Column(
                modifier = Modifier
                    .height(100.dp)
                    .width(180.dp)
                    .padding(top = 18.dp),
                horizontalAlignment = Alignment.Start
            ) {
                Row(
                    verticalAlignment = Alignment.CenterVertically
                ) {
                    Text(
                        text = menuList.menuK,
                        color = Color(0xff191919),
                        fontWeight = FontWeight.Bold,
                        fontSize = 16.sp
                    )
                    Spacer(modifier = Modifier.width(3.dp))
                    when (menuList.tag) {
                        Tag.NONE -> {}
                        Tag.BEST -> Text(
                            text = "Best",
                            color = Color.Red,
                            fontStyle = FontStyle.Italic,
                            fontSize = 8.sp
                        )

                        Tag.NEW -> Text(
                            text = "New",
                            color = Color(0xff53f4c6),
                            fontStyle = FontStyle.Italic,
                            fontSize = 8.sp
                        )

                    }
                }
                Text(
                    text = menuList.menuEn,
                    color = Color(0xffd9d9d9),
                    fontSize = 15.sp,
                    maxLines = 1
                )
                Text(
                    text = menuList.price,
                    color = Color(0xff191919),
                    fontSize = 18.sp,
                    fontWeight = FontWeight.Bold
                )
            }

        }
    }
}
```

---------------------
MenuInfoActivity
```kotlin
class MenuInfoActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContent {
            StarbucksAppTheme {
                Surface(
                    modifier = Modifier.fillMaxSize(),
                    color = Color.White
                ) {
                    val menu = intent.getSerializableExtra("menu") as? List<Menu>
                    val currentIndex = intent.getIntExtra("currentIndex", 0)
                    val menuInfoList = listOf(
                        MenuInfo(
                            menuK = "아이스 카페 아메리카노",
                            menuEn = "Iced Caffe Americano",
                            price = 4500,
                            picture = R.drawable.americano,
                            tag = Tag.BEST,
                            menuDetail = "진한 에스프레소에 시원한 정수물과 얼음을 더하여 " +
                                    "스타벅스의 깔끔하고 강렬한 에스프레소를 가장 " +
                                    "부드럽고 시원하게 즐길 수 있는 커피"
                        ),
                        MenuInfo(
                            menuK = "아이스 카페 라떼",
                            menuEn = "Iced Caffe Latte",
                            price = 5000,
                            picture = R.drawable.cafelatte,
                            tag = Tag.BEST,
                            menuDetail = "풍부하고 진한 농도의 에스프레소가 시원한 우유와 " +
                                    "얼음을 만나 고소함과 시원함을 즐길 수 있는 대표적인 카페 라떼"
                        ),
                        MenuInfo(
                            menuK = "씨솔트 카라멜 콜드 브루",
                            menuEn = "Sea Salt Caramel Cold Brew",
                            price = 6300,
                            picture = R.drawable.caramelcoldbrew,
                            tag = Tag.NEW,
                            menuDetail = "구름 처럼 부드러운 씨솔트 폼과 번트 카라멜의 중독성 강" +
                                    "한 단짠단짠 조합의 콜드브루\n한번 맛보면 자꾸 생각나는 매력" +
                                    "적인 음료"
                        ),
                        MenuInfo(
                            menuK = "아이스 자몽 허니 블랙 티",
                            menuEn = "Iced Grapefruit Honey Black Tea",
                            price = 5700,
                            picture = R.drawable.honeytea,
                            tag = Tag.BEST,
                            menuDetail = "새콤한 자몽과 달콤한 꿀이 깊고 그윽한 풍미와 티바나 " +
                                    "블랙 티의 조화\n\n한정된 기간 동안 판매하는 트렌타 사이즈로 " +
                                    "즐겨 보세요."
                        ),
                        MenuInfo(
                            menuK = "콜드 브루",
                            menuEn = "Cold Brew",
                            price = 6900,
                            picture = R.drawable.coldbrew,
                            tag = Tag.NEW,
                            menuDetail = "스타벅스 바리스타의 정성으로 탄생한 콜드브루!\n콜드 브" +
                                    "루 전용 원두를 차가운 물로 추출하여 한정된 양만 제공됩니다. 실크" +
                                    "같이 부드럽고 그윽한 초콜릿 풍미의 콜드브루를 만나보세요!\n\n한정" +
                                    "된 기간 동안 판매하는 트렌타 사이즈로 즐겨 보세요."
                        )
                    )

                    when (currentIndex) {
                        0 -> {
                            MenuInfoView(menuInfoList[0])
                        }

                        1 -> {
                            MenuInfoView(menuInfoList[1])
                        }

                        2 -> {
                            MenuInfoView(menuInfoList[2])
                        }

                        3 -> {
                            MenuInfoView(menuInfoList[3])
                        }

                        4 -> {
                            MenuInfoView(menuInfoList[4])
                        }

                        else -> {
                            Text(text = "No Information")
                        }
                    }
                }
            }
        }
    }
}

data class MenuInfo(
    val menuK: String,
    val menuEn: String,
    val price: Int,
    val picture: Int,
    val tag: Tag = Tag.NONE,
    val menuDetail: String,
) {
    val priceFormat: String
        get() {
            val dotThree = DecimalFormat("#,###")
            return "${dotThree.format(price)}원"
        }
}


@Composable
fun MenuInfoView(menuInfoList: MenuInfo) {
    Surface(
        modifier = Modifier.fillMaxSize(),
        color = Color.White

    ) {
        val context = LocalContext.current
        var clickHot by remember { mutableStateOf(false) }
        var clickIced by remember { mutableStateOf(false) }
        var clickOrder by remember { mutableStateOf(false) }

        Box {
            Column {
                Column(
                    modifier = Modifier
                        .verticalScroll(rememberScrollState())
                        .weight(1f)
                ) {
                    Surface(
                        modifier = Modifier
                            .fillMaxWidth()
                            .height(340.dp)
                    ) {
                        Image(
                            painter = painterResource(id = menuInfoList.picture),
                            contentDescription = "",
                            contentScale = ContentScale.FillWidth
                        )
                    }
                    Box(
                        modifier = Modifier
                            .height(4.dp)
                            .fillMaxWidth()
                            .background(
                                brush = gra(
                                    "v",
                                    "shadow"
                                ) as Brush,
                                shape = RectangleShape
                            )
                    )
                    Spacer(modifier = Modifier.height(8.dp))
                    Column(
                        modifier = Modifier.padding(30.dp)
                    ) {
                        Row(
                            verticalAlignment = Alignment.CenterVertically,
                            modifier = Modifier.padding(0.dp)
                        ) {
                            Text(
                                text = menuInfoList.menuK,
                                fontSize = 25.sp,
                                fontWeight = FontWeight.Bold
                            )
                            Spacer(modifier = Modifier.width(8.dp))
                            when (menuInfoList.tag) {
                                Tag.NONE -> {}
                                Tag.BEST -> Text(
                                    text = "Best",
                                    color = Color.Red,
                                    fontStyle = FontStyle.Italic,
                                    fontSize = 10.sp
                                )

                                Tag.NEW -> Text(
                                    text = "New",
                                    color = Color(0xff53f4c6),
                                    fontStyle = FontStyle.Italic,
                                    fontSize = 10.sp
                                )
                            }
                        }
                        Text(
                            text = menuInfoList.menuEn,
                            color = Color(0xffd9d9d9),
                            fontSize = 15.sp,
                            fontWeight = FontWeight.Light
                        )
                        Spacer(modifier = Modifier.height(14.dp))
                        Text(
                            text = menuInfoList.menuDetail,
                            color = Color(0xff767676),
                            lineHeight = 19.sp
                        )
                        Spacer(modifier = Modifier.height(22.dp))
                        Text(
                            text = menuInfoList.priceFormat,
                            fontWeight = FontWeight.Bold,
                            fontSize = 24.sp
                        )
                        Spacer(modifier = Modifier.height(22.dp))
                        Box(
                            modifier = Modifier
                                .width(320.dp)
                                .height(40.dp)
                                .background(color = Color.White, shape = CircleShape)
                                .border(
                                    width = 2.dp,
                                    brush = gra(
                                        "R",
                                        "shadow"
                                    ) as Brush,
                                    shape = CircleShape
                                ),
                        ) {
                            Row(
                                horizontalArrangement = Arrangement.SpaceBetween
                            ) {
                                Button(
                                    onClick = {
                                        clickHot = true
                                        clickIced = false
                                    },
                                    colors = ButtonDefaults.buttonColors(
                                        if (clickHot) Color(
                                            0xfff24405
                                        ) else Color.White
                                    ),
                                    shape = RoundedCornerShape(
                                        topStart = 30.dp,
                                        bottomStart = 30.dp
                                    ),
                                    modifier = Modifier.width(159.dp)
                                ) {
                                    Text(
                                        text = "HOT",
                                        textAlign = TextAlign.Center,
                                        color = if (clickHot) Color.White else Color(0xff555555),
                                        fontSize = 15.sp,
                                        fontWeight = FontWeight.Bold
                                    )
                                }
                                Button(
                                    onClick = {
                                        clickHot = false
                                        clickIced = true
                                    },
                                    colors = ButtonDefaults.buttonColors(
                                        if (clickIced) Color(
                                            0xff0460d9
                                        ) else Color.White
                                    ),
                                    shape = RoundedCornerShape(topEnd = 30.dp, bottomEnd = 30.dp),
                                    modifier = Modifier.width(159.dp)
                                ) {
                                    Text(
                                        text = "ICED",
                                        textAlign = TextAlign.Center,
                                        color = if (clickIced) Color.White else Color(0xff555555),
                                        fontSize = 15.sp,
                                        fontWeight = FontWeight.Bold
                                    )
                                }
                            }
                        }
                        Spacer(modifier = Modifier.height(22.dp))
                        Surface(
                            modifier = Modifier.height(90.dp),
                            color = Color(0xfff9f9f9),
                        ) {
                            Text(
                                text = "블론드/디카페인 커피 Tab에서\n블론드, 디카페인, 1/2디카페인" +
                                        " 아메리카노를 주문할 수 있습니다.",
                                color = Color(0xff767676),
                                lineHeight = 19.sp,
                                modifier = Modifier.padding(15.dp)
                            )
                        }
                    }
                }
                Column(
                    horizontalAlignment = Alignment.CenterHorizontally,
                    verticalArrangement = Arrangement.Center,
                    modifier = Modifier
                        .fillMaxWidth()
                        .height(90.dp)
                ) {
                    Box(
                        modifier = Modifier
                            .height(4.dp)
                            .fillMaxWidth()
                            .background(
                                brush = gra(
                                    "v",
                                    "shadowReverse"
                                ) as Brush,
                                shape = RectangleShape
                            )
                    )
                    Spacer(modifier = Modifier.height(10.dp))
                    Button(
                        onClick = {
                            clickOrder = true
                        },
                        modifier = Modifier.width(350.dp),
                        colors = ButtonDefaults.buttonColors(Color(0xff1ca557))
                    ) {
                        Text(
                            text = "주문하기",
                            color = Color.White,
                            fontSize = 18.sp
                        )
                    }
                }
            }
            Button(
                onClick = {
                    val intent = Intent(context, MainActivity::class.java)
                    context.startActivity(intent)
                },
                colors = ButtonDefaults.buttonColors(Color(0xff1e3932)),
                modifier = Modifier
                    .size(50.dp)
                    .padding(top = 10.dp)
                    .padding(start = 10.dp),
                contentPadding = PaddingValues(0.dp),
                shape = CircleShape
            ) {
                Text(
                    text = "〈",
                    fontWeight = FontWeight.Bold,
                    color = Color(0xffffffff),
                    textAlign = TextAlign.Center,
                    modifier = Modifier.padding(end = 5.dp)
                )
            }
            Column(
                horizontalAlignment = Alignment.End,
                modifier = Modifier.fillMaxWidth()
            ) {
                Button(
                    onClick = { },
                    colors = ButtonDefaults.buttonColors(Color(0xff1e3932)),
                    modifier = Modifier
                        .size(50.dp)
                        .padding(top = 10.dp)
                        .padding(end = 10.dp),
                    contentPadding = PaddingValues(0.dp),
                    shape = CircleShape
                ) {
                    Icon(
                        painterResource(id = R.drawable.shared),
                        contentDescription = "SharedIcon",
                        tint = Color(0xffffffff),
                        modifier = Modifier.size(20.dp)
                    )
                }
                Spacer(modifier = Modifier.height(240.dp))
                Button(
                    onClick = { },
                    colors = ButtonDefaults.buttonColors(Color(0xff1e3932)),
                    modifier = Modifier
                        .size(50.dp)
                        .padding(top = 10.dp)
                        .padding(end = 10.dp),
                    contentPadding = PaddingValues(0.dp),
                    shape = CircleShape
                ) {
                    Icon(
                        painterResource(id = R.drawable.zoom),
                        contentDescription = "SharedIcon",
                        tint = Color(0xffffffff),
                        modifier = Modifier.size(20.dp)
                    )
                }
            }
        }
        if (clickOrder) {
            OrderPage(menuInfoList)
        }
    }
}

//@Composable
//fun AppNavigation() {
//    val navController = rememberNavController()
//
//    NavHost(navController = navController, startDestination = "main") {
//        composable("main") {
//            MenuInfoView(menuInfoList[currentIndex], navController)
//        }
//        composable("orderPage") {
//            OrderPage()
//        }
//
//    }
//}

@Composable
fun OrderPage(menuInfoList: MenuInfo) {
    var clickOption by remember { mutableStateOf(true) }
    var count by remember { mutableStateOf(1) }
    var clickTall by remember { mutableStateOf(true) }
    var clickGrande by remember { mutableStateOf(false) }
    var clickVenti by remember { mutableStateOf(false) }
    var clickIn by remember { mutableStateOf(false) }
    var clickPersonal by remember { mutableStateOf(false) }
    var clickTake by remember { mutableStateOf(false) }
    var totalPrice = menuInfoList.price
    var clickHeart by remember { mutableStateOf(false) }

    fun toatalPriceFormat(totalPrice: Int): String {
        val dotThree = DecimalFormat("#,###")
        return "${dotThree.format(totalPrice)}원"
    }

    Column(
        verticalArrangement = Arrangement.Bottom
    ) {
        Surface(
            modifier = Modifier
                .fillMaxWidth()
                .height(770.dp),
            color = Color.White
        ) {
            Column {
                Column(
                    horizontalAlignment = Alignment.CenterHorizontally,
                    modifier = Modifier
                        .verticalScroll(rememberScrollState())
                        .weight(1f)
                        .padding(15.dp)
                ) {
                    Spacer(modifier = Modifier.height(20.dp))
                    Text(
                        text = menuInfoList.menuK,
                        fontSize = 25.sp
                    )
                    Spacer(modifier = Modifier.height(20.dp))
                    Surface(
                        modifier = Modifier
                            .fillMaxWidth()
                            .height(35.dp),
                        color = Color(0xffcfeae2)
                    ) {
                        Text(
                            text = "환경을 위해 사용 줄이기에 동참해주세요",
                            color = Color(0xff1ca557),
                            fontSize = 15.sp,
                            modifier = Modifier
                                .fillMaxSize()
                                .padding(top = 8.dp),
                            textAlign = TextAlign.Center,
                            fontWeight = FontWeight.Bold
                        )
                    }
                    Spacer(modifier = Modifier.height(35.dp))
                    Column(
                        horizontalAlignment = Alignment.Start
                    ) {
                        Text(
                            text = "사이즈",
                            fontSize = 25.sp,
                            fontWeight = FontWeight.Bold
                        )
                        Spacer(modifier = Modifier.height(10.dp))
                        Row(
                            horizontalArrangement = Arrangement.SpaceBetween
                        ) {
                            Button(
                                onClick = {
                                    clickTall = true
                                    clickGrande = false
                                    clickVenti = false
                                },
                                modifier = Modifier
                                    .width(110.dp)
                                    .height(140.dp),
                                shape = RoundedCornerShape(10.dp),
                                colors = ButtonDefaults.buttonColors(Color.White),
                                border = BorderStroke(
                                    if (clickTall) 2.dp else 1.dp,
                                    if (clickTall) Color(0xff1ca557) else Color(0xffd9d9d9)
                                )
                            ) {
                                Column(
                                    horizontalAlignment = Alignment.CenterHorizontally
                                ) {
                                    if (clickTall) {
                                        Icon(
                                            painterResource(id = R.drawable.fillcup),
                                            contentDescription = "selectTall",
                                            tint = Color(0xff1ca557),
                                            modifier = Modifier.size(30.dp)
                                        )
                                    } else {
                                        Icon(
                                            painterResource(id = R.drawable.cup),
                                            contentDescription = "Tall",
                                            tint = Color(0xffd9d9d9),
                                            modifier = Modifier.size(30.dp)
                                        )
                                    }
                                    Spacer(modifier = Modifier.height(10.dp))
                                    Text(
                                        text = "Tall",
                                        fontWeight = if (clickTall) FontWeight.Bold else FontWeight.Normal,
                                        color = if (clickTall) Color.Black else Color(0xffc9c9c9),
                                        fontSize = 16.sp
                                    )
                                    Text(
                                        text = "355ml",
                                        color = if (clickTall) Color.Black else Color(0xffc9c9c9),
                                        fontSize = 16.sp
                                    )
                                }
                            }
                            Spacer(modifier = Modifier.width(5.dp))
                            Button(
                                onClick = {
                                    clickTall = false
                                    clickGrande = true
                                    clickVenti = false
                                },
                                modifier = Modifier
                                    .width(110.dp)
                                    .height(140.dp),
                                shape = RoundedCornerShape(10.dp),
                                colors = ButtonDefaults.buttonColors(Color.White),
                                border = BorderStroke(
                                    if (clickGrande) 2.dp else 1.dp,
                                    if (clickGrande) Color(0xff1ca557) else Color(0xffd9d9d9)
                                )
                            ) {
                                Column {
                                    if (clickGrande) {
                                        Icon(
                                            painterResource(id = R.drawable.fillcup),
                                            contentDescription = "selectGrande",
                                            tint = Color(0xff1ca557),
                                            modifier = Modifier.size(40.dp)
                                        )
                                    } else {
                                        Icon(
                                            painterResource(id = R.drawable.cup),
                                            contentDescription = "Grande",
                                            tint = Color(0xffd9d9d9),
                                            modifier = Modifier.size(40.dp)
                                        )
                                    }
                                    Spacer(modifier = Modifier.height(6.dp))
                                    Text(
                                        text = "Grande",
                                        fontWeight = if (clickGrande) FontWeight.Bold else FontWeight.Normal,
                                        color = if (clickGrande) Color.Black else Color(0xffc9c9c9),
                                        fontSize = 16.sp
                                    )
                                    Text(
                                        text = "473ml",
                                        color = if (clickGrande) Color.Black else Color(0xffc9c9c9),
                                        fontSize = 16.sp
                                    )
                                }
                            }
                            Spacer(modifier = Modifier.width(5.dp))
                            Button(
                                onClick = {
                                    clickTall = false
                                    clickGrande = false
                                    clickVenti = true
                                },
                                modifier = Modifier
                                    .width(110.dp)
                                    .height(140.dp),
                                shape = RoundedCornerShape(10.dp),
                                colors = ButtonDefaults.buttonColors(Color.White),
                                border = BorderStroke(
                                    if (clickVenti) 2.dp else 1.dp,
                                    if (clickVenti) Color(0xff1ca557) else Color(0xffd9d9d9)
                                )
                            ) {
                                Column {
                                    if (clickVenti) {
                                        Icon(
                                            painterResource(id = R.drawable.fillcup),
                                            contentDescription = "selectVenti",
                                            tint = Color(0xff1ca557),
                                            modifier = Modifier.size(50.dp)
                                        )
                                    } else {
                                        Icon(
                                            painterResource(id = R.drawable.cup),
                                            contentDescription = "Venti",
                                            tint = Color(0xffd9d9d9),
                                            modifier = Modifier.size(50.dp)
                                        )
                                    }
                                    Spacer(modifier = Modifier.height(2.dp))
                                    Text(
                                        text = "Venti",
                                        fontWeight = if (clickVenti) FontWeight.Bold else FontWeight.Normal,
                                        color = if (clickVenti) Color.Black else Color(0xffc9c9c9),
                                        fontSize = 16.sp
                                    )
                                    Text(
                                        text = "591ml",
                                        color = if (clickVenti) Color.Black else Color(0xffc9c9c9),
                                        fontSize = 16.sp
                                    )
                                }
                            }
                        }
                    }
                    Spacer(modifier = Modifier.height(35.dp))
                    Column(
                        horizontalAlignment = Alignment.Start
                    ) {
                        Text(
                            text = "컵 선택",
                            fontSize = 25.sp,
                            fontWeight = FontWeight.Bold
                        )
                        Spacer(modifier = Modifier.height(10.dp))
                        Row(
                            horizontalArrangement = Arrangement.SpaceBetween
                        ) {
                            Button(
                                onClick = {
                                    clickIn = true
                                    clickPersonal = false
                                    clickTake = false
                                },
                                shape = RoundedCornerShape(topStart = 30.dp, bottomStart = 30.dp),
                                colors = ButtonDefaults.buttonColors(Color.White),
                                border = BorderStroke(
                                    if (clickIn) 2.dp else 1.dp,
                                    if (clickIn) Color(0xff1ca557) else Color(0xffd9d9d9)
                                ),
                                modifier = Modifier
                                    .height(60.dp)
                                    .width(113.dp)
                            ) {
                                Text(
                                    text = "매장컵",
                                    color = if (clickIn) Color.Black else Color(0xffc9c9c9),
                                    fontSize = 16.sp,
                                    fontWeight = FontWeight.Bold
                                )
                            }
                            Button(
                                onClick = {
                                    clickIn = false
                                    clickPersonal = true
                                    clickTake = false
                                },
                                shape = RectangleShape,
                                colors = ButtonDefaults.buttonColors(Color.White),
                                border = BorderStroke(
                                    if (clickPersonal) 2.dp else 1.dp,
                                    if (clickPersonal) Color(0xff1ca557) else Color(0xffd9d9d9)
                                ),
                                modifier = Modifier
                                    .height(60.dp)
                                    .width(113.dp)
                            ) {
                                Text(
                                    text = "개인컵",
                                    color = if (clickPersonal) Color.Black else Color(0xffc9c9c9),
                                    fontSize = 16.sp,
                                    fontWeight = FontWeight.Bold
                                )
                            }
                            Button(
                                onClick = {
                                    clickIn = false
                                    clickPersonal = false
                                    clickTake = true
                                },
                                shape = RoundedCornerShape(topEnd = 30.dp, bottomEnd = 30.dp),
                                colors = ButtonDefaults.buttonColors(Color.White),
                                border = BorderStroke(
                                    if (clickTake) 2.dp else 1.dp,
                                    if (clickTake) Color(0xff1ca557) else Color(0xffd9d9d9)
                                ),
                                modifier = Modifier
                                    .height(60.dp)
                                    .width(113.dp)
                            ) {
                                Text(
                                    text = "일회용컵",
                                    color = if (clickTake) Color.Black else Color(0xffc9c9c9),
                                    fontSize = 16.sp,
                                    fontWeight = FontWeight.Bold
                                )
                            }
                        }
                    }
                    Spacer(modifier = Modifier.height(50.dp))
                    Divider(thickness = 1.dp, color = Color(0xffd9d9d9))
                    Spacer(modifier = Modifier.height(8.dp))
                    Button(
                        onClick = { clickOption = !clickOption },
                        colors = ButtonDefaults.buttonColors(Color.White),
                        modifier = Modifier
                            .fillMaxWidth()
                            .height(60.dp),
                        contentPadding = PaddingValues(0.dp)
                    ) {
                        Row(
                            horizontalArrangement = Arrangement.SpaceBetween
                        ) {
                            Text(
                                text = "퍼스널옵션",
                                fontSize = 25.sp,
                                fontWeight = FontWeight.Bold,
                                modifier = Modifier.width(300.dp),
                                textAlign = TextAlign.Start,
                                color = Color.Black
                            )
                            Text(
                                text = if (clickOption) "▽" else "△",
                                color = Color(0xff767676),
                                fontSize = 22.sp,
                                textAlign = TextAlign.End,
                                modifier = Modifier
                                    .width(100.dp)
                                    .padding(end = 15.dp)
                            )
                        }
                    }
                }
                if (!clickOption) {
                    PersonalOption(
                        clickTall,
                        clickGrande,
                        clickVenti,
                        clickIn,
                        clickPersonal,
                        clickTake,
                        clickOption
                    ) {
                        clickOption = true
                    }
                }
                Column(
                    modifier = Modifier
                        .fillMaxWidth()
                        .height(120.dp)
                ) {
                    Box(
                        modifier = Modifier
                            .height(4.dp)
                            .fillMaxWidth()
                            .background(
                                brush = gra(
                                    "v",
                                    "shadowReverse"
                                ) as Brush,
                                shape = RectangleShape
                            )
                    )
                    Spacer(modifier = Modifier.height(10.dp))
                    Column {
                        Row {
                            Spacer(modifier = Modifier.width(10.dp))
                            IconButton(
                                onClick = {
                                    if (count > 1) {
                                        count--
                                        totalPrice -= menuInfoList.price
                                    }
                                },
                                enabled = count > 1,
                                modifier = Modifier
                                    .size(30.dp)
                                    .padding(top = 8.dp)
                            ) {
                                Icon(
                                    painterResource(id = R.drawable.minus),
                                    contentDescription = "minus",
                                    modifier = Modifier.size(30.dp),
                                    tint = if (count > 1) Color(0xff767676) else Color(0xffd9d9d9)
                                )
                            }
                            Spacer(modifier = Modifier.width(20.dp))
                            Text(
                                text = count.toString(),
                                fontWeight = FontWeight.Bold,
                                color = Color.Black,
                                fontSize = 25.sp
                            )
                            Spacer(modifier = Modifier.width(20.dp))
                            IconButton(
                                onClick = {
                                    count++
                                    totalPrice += menuInfoList.price
                                },
                                modifier = Modifier
                                    .size(30.dp)
                                    .padding(top = 8.dp)
                            ) {
                                Icon(
                                    painterResource(id = R.drawable.plus),
                                    contentDescription = "plus",
                                    modifier = Modifier.size(30.dp),
                                    tint = Color(0xff767676)
                                )
                            }
                            Text(
                                text = toatalPriceFormat(totalPrice),
                                fontWeight = FontWeight.Bold,
                                color = Color.Black,
                                fontSize = 25.sp,
                                textAlign = TextAlign.End,
                                modifier = Modifier
                                    .width(300.dp)
                                    .padding(end = 20.dp)
                            )
                        }
                        Spacer(modifier = Modifier.height(10.dp))
                        Row {
                            Spacer(modifier = Modifier.width(10.dp))
                            Column {
                                Spacer(modifier = Modifier.height(8.dp))
                                IconButton(
                                    onClick = {
                                        clickHeart = !clickHeart
                                    },
                                    modifier = Modifier
                                        .size(30.dp)
                                ) {
                                    if (clickHeart) {
                                        Icon(
                                            painterResource(id = R.drawable.fillheart),
                                            contentDescription = "fillheart",
                                            modifier = Modifier
                                                .size(30.dp),
                                            tint = Color.Red
                                        )
                                    } else {
                                        Icon(
                                            painterResource(id = R.drawable.heart),
                                            contentDescription = "heart",
                                            modifier = Modifier
                                                .size(30.dp),
                                            tint = Color(0xff767676)
                                        )
                                    }
                                }
                            }
                            Spacer(modifier = Modifier.width(40.dp))
                            Button(
                                onClick = { /*TODO*/ },
                                modifier = Modifier.width(90.dp),
                                colors = ButtonDefaults.buttonColors(Color.White),
                                border = BorderStroke(1.dp, Color(0xff1ca557))
                            ) {
                                Text(
                                    text = "담기",
                                    color = Color(0xff1ca557),
                                    fontSize = 18.sp
                                )
                            }
                            Spacer(modifier = Modifier.width(15.dp))
                            Button(
                                onClick = { },
                                modifier = Modifier.width(180.dp),
                                colors = ButtonDefaults.buttonColors(Color(0xff1ca557))
                            ) {
                                Text(
                                    text = "주문하기",
                                    color = Color.White,
                                    fontSize = 18.sp
                                )
                            }
                            Spacer(modifier = Modifier.width(5.dp))
                        }
                    }
                }
            }
        }
    }
}

@Composable
fun PersonalOption(
    clickTall: Boolean,
    clickGrande: Boolean,
    clickVenti: Boolean,
    clickIn: Boolean,
    clickPersonal: Boolean,
    clickTake: Boolean,
    clickOption: Boolean,
    onBack: () -> Unit,
) {
    val choiceCup = when {
        clickIn -> "매장컵"
        clickPersonal -> "개인컵"
        clickTake -> "일회용컵"
        !clickIn && !clickPersonal && !clickTake -> "선택 안함"
        else -> ""
    }
//    var clickOption by remember { mutableStateOf(false) }

    Column(
        verticalArrangement = Arrangement.Bottom
    ) {
        Surface(
            modifier = Modifier
                .fillMaxWidth()
                .height(570.dp),
            color = Color.White
        ) {
            Column(
                horizontalAlignment = Alignment.CenterHorizontally
            ) {
                Row(
                    modifier = Modifier.height(50.dp),
                    verticalAlignment = Alignment.CenterVertically
                ) {
                    when {
                        clickTall -> {
                            Icon(
                                painterResource(id = R.drawable.fillcup),
                                contentDescription = "Tall",
                                tint = Color(0xff1ca557),
                                modifier = Modifier.size(30.dp)
                            )
                            Spacer(modifier = Modifier.width(15.dp))
                            Text(
                                text = "Tall (355ml)",
                                fontWeight = FontWeight.Bold,
                                color = Color.Black,
                                fontSize = 16.sp
                            )
                        }

                        clickGrande -> {
                            Icon(
                                painterResource(id = R.drawable.fillcup),
                                contentDescription = "selectGrande",
                                tint = Color(0xff1ca557),
                                modifier = Modifier.size(40.dp)
                            )
                            Spacer(modifier = Modifier.width(15.dp))
                            Text(
                                text = "Grande (473ml)",
                                fontWeight = FontWeight.Bold,
                                color = Color.Black,
                                fontSize = 16.sp
                            )
                        }

                        clickVenti -> {
                            Icon(
                                painterResource(id = R.drawable.fillcup),
                                contentDescription = "selectVenti",
                                tint = Color(0xff1ca557),
                                modifier = Modifier.size(50.dp)
                            )
                            Spacer(modifier = Modifier.width(15.dp))
                            Text(
                                text = "Venti (591ml)",
                                fontWeight = FontWeight.Bold,
                                color = Color.Black,
                                fontSize = 16.sp
                            )
                        }
                    }
                    Spacer(modifier = Modifier.width(15.dp))
                    Surface(
                        color = Color(0xffd9d9d9),
                        modifier = Modifier
                            .height(20.dp)
                            .width(3.dp)
                            .padding(top = 5.dp),
                        shape = RoundedCornerShape(30.dp)
                    ) { }
                    Spacer(modifier = Modifier.width(15.dp))
                    Text(text = "컵 선택", color = Color(0xff767676), fontSize = 16.sp)
                    Spacer(modifier = Modifier.width(5.dp))
                    Text(
                        text = choiceCup,
                        fontWeight = FontWeight.Bold,
                        color = Color.Black,
                        fontSize = 16.sp
                    )
                }
                Column {
                    Surface(
                        color = Color(0xfff9f9f9)
                    ) {
                        Column(
                            modifier = Modifier.padding(15.dp)
                        ) {
                            Button(
                                onClick = {
                                    onBack()
                                },
                                colors = ButtonDefaults.buttonColors(Color(0xfff9f9f9)),
                                modifier = Modifier
                                    .fillMaxWidth()
                                    .height(60.dp),
                                contentPadding = PaddingValues(0.dp)
                            ) {
                                Row(
                                    horizontalArrangement = Arrangement.SpaceBetween
                                ) {
                                    Text(
                                        text = "퍼스널옵션",
                                        fontSize = 25.sp,
                                        fontWeight = FontWeight.Bold,
                                        modifier = Modifier.width(300.dp),
                                        textAlign = TextAlign.Start,
                                        color = Color.Black
                                    )
                                    Text(
                                        text = if (clickOption) "▽" else "△",
                                        color = Color(0xff767676),
                                        fontSize = 22.sp,
                                        textAlign = TextAlign.End,
                                        modifier = Modifier
                                            .width(100.dp)
                                            .padding(end = 15.dp)
                                    )
                                }
                            }
                            Button(
                                onClick = { },
                                colors = ButtonDefaults.buttonColors(Color(0xfff9f9f9)),
                                modifier = Modifier
                                    .fillMaxWidth()
                                    .height(80.dp),
                                contentPadding = PaddingValues(0.dp),
                                shape = RoundedCornerShape(10.dp)
                            ) {
                                Row(
                                    horizontalArrangement = Arrangement.SpaceBetween,
                                    verticalAlignment = Alignment.CenterVertically
                                ) {
                                    Column {
                                        Text(
                                            text = "커피",
                                            fontSize = 20.sp,
                                            modifier = Modifier.width(300.dp),
                                            textAlign = TextAlign.Start,
                                            fontWeight = FontWeight.SemiBold,
                                            color = Color.Black
                                        )
                                        Text(
                                            text = "에스프레소 샷 2",
                                            fontSize = 18.sp,
                                            modifier = Modifier.width(300.dp),
                                            textAlign = TextAlign.Start,
                                            color = Color(0xff838383)
                                        )
                                    }
                                    Text(
                                        text = "〉",
                                        color = Color(0xff767676),
                                        fontSize = 25.sp,
                                        textAlign = TextAlign.End,
                                        modifier = Modifier
                                            .width(100.dp)
                                            .padding(end = 15.dp)
                                    )
                                }
                            }
                            Divider(thickness = 2.dp, color = Color(0xffd9d9d9))
                            Button(
                                onClick = { },
                                colors = ButtonDefaults.buttonColors(Color(0xfff9f9f9)),
                                modifier = Modifier
                                    .fillMaxWidth()
                                    .height(60.dp),
                                contentPadding = PaddingValues(0.dp),
                                shape = RoundedCornerShape(10.dp)
                            ) {
                                Row(
                                    horizontalArrangement = Arrangement.SpaceBetween,
                                    verticalAlignment = Alignment.CenterVertically
                                ) {
                                    Text(
                                        text = "시럽",
                                        fontSize = 20.sp,
                                        modifier = Modifier.width(300.dp),
                                        textAlign = TextAlign.Start,
                                        fontWeight = FontWeight.SemiBold,
                                        color = Color.Black
                                    )
                                    Text(
                                        text = "〉",
                                        color = Color(0xff767676),
                                        fontSize = 25.sp,
                                        textAlign = TextAlign.End,
                                        modifier = Modifier
                                            .width(100.dp)
                                            .padding(end = 15.dp)
                                    )
                                }
                            }
                            Divider(thickness = 2.dp, color = Color(0xffd9d9d9))
                            Button(
                                onClick = { },
                                colors = ButtonDefaults.buttonColors(Color(0xfff9f9f9)),
                                modifier = Modifier
                                    .fillMaxWidth()
                                    .height(80.dp),
                                contentPadding = PaddingValues(0.dp),
                                shape = RoundedCornerShape(10.dp)
                            ) {
                                Row(
                                    horizontalArrangement = Arrangement.SpaceBetween,
                                    verticalAlignment = Alignment.CenterVertically
                                ) {
                                    Column {
                                        Text(
                                            text = "베이스",
                                            fontSize = 20.sp,
                                            modifier = Modifier.width(300.dp),
                                            textAlign = TextAlign.Start,
                                            fontWeight = FontWeight.SemiBold,
                                            color = Color.Black
                                        )
                                        Text(
                                            text = "물",
                                            fontSize = 18.sp,
                                            modifier = Modifier.width(300.dp),
                                            textAlign = TextAlign.Start,
                                            color = Color(0xff838383)
                                        )
                                    }
                                    Text(
                                        text = "〉",
                                        color = Color(0xff767676),
                                        fontSize = 25.sp,
                                        textAlign = TextAlign.End,
                                        modifier = Modifier
                                            .width(100.dp)
                                            .padding(end = 15.dp)
                                    )
                                }
                            }
                            Divider(thickness = 2.dp, color = Color(0xffd9d9d9))
                            Button(
                                onClick = { },
                                colors = ButtonDefaults.buttonColors(Color(0xfff9f9f9)),
                                modifier = Modifier
                                    .fillMaxWidth()
                                    .height(80.dp),
                                contentPadding = PaddingValues(0.dp),
                                shape = RoundedCornerShape(10.dp)
                            ) {
                                Row(
                                    horizontalArrangement = Arrangement.SpaceBetween,
                                    verticalAlignment = Alignment.CenterVertically
                                ) {
                                    Column {
                                        Text(
                                            text = "얼음",
                                            fontSize = 20.sp,
                                            modifier = Modifier.width(300.dp),
                                            textAlign = TextAlign.Start,
                                            fontWeight = FontWeight.SemiBold,
                                            color = Color.Black
                                        )
                                        Text(
                                            text = "얼음",
                                            fontSize = 18.sp,
                                            modifier = Modifier.width(300.dp),
                                            textAlign = TextAlign.Start,
                                            color = Color(0xff838383)
                                        )
                                    }
                                    Text(
                                        text = "〉",
                                        color = Color(0xff767676),
                                        fontSize = 25.sp,
                                        textAlign = TextAlign.End,
                                        modifier = Modifier
                                            .width(100.dp)
                                            .padding(end = 15.dp)
                                    )
                                }
                            }
                            Divider(thickness = 2.dp, color = Color(0xffd9d9d9))
                            Button(
                                onClick = { },
                                colors = ButtonDefaults.buttonColors(Color(0xfff9f9f9)),
                                modifier = Modifier
                                    .fillMaxWidth()
                                    .height(60.dp),
                                contentPadding = PaddingValues(0.dp),
                                shape = RoundedCornerShape(10.dp)
                            ) {
                                Row(
                                    horizontalArrangement = Arrangement.SpaceBetween,
                                    verticalAlignment = Alignment.CenterVertically
                                ) {
                                    Text(
                                        text = "휘핑크림",
                                        fontSize = 20.sp,
                                        modifier = Modifier.width(300.dp),
                                        textAlign = TextAlign.Start,
                                        fontWeight = FontWeight.SemiBold,
                                        color = Color.Black
                                    )
                                    Text(
                                        text = "〉",
                                        color = Color(0xff767676),
                                        fontSize = 25.sp,
                                        textAlign = TextAlign.End,
                                        modifier = Modifier
                                            .width(100.dp)
                                            .padding(end = 15.dp)
                                    )
                                }
                            }
                            Divider(thickness = 2.dp, color = Color(0xffd9d9d9))
                            Button(
                                onClick = { },
                                colors = ButtonDefaults.buttonColors(Color(0xfff9f9f9)),
                                modifier = Modifier
                                    .fillMaxWidth()
                                    .height(60.dp),
                                contentPadding = PaddingValues(0.dp),
                                shape = RoundedCornerShape(10.dp)
                            ) {
                                Row(
                                    horizontalArrangement = Arrangement.SpaceBetween,
                                    verticalAlignment = Alignment.CenterVertically
                                ) {
                                    Text(
                                        text = "드리즐",
                                        fontSize = 20.sp,
                                        modifier = Modifier.width(300.dp),
                                        textAlign = TextAlign.Start,
                                        fontWeight = FontWeight.SemiBold,
                                        color = Color.Black
                                    )
                                    Text(
                                        text = "〉",
                                        color = Color(0xff767676),
                                        fontSize = 25.sp,
                                        textAlign = TextAlign.End,
                                        modifier = Modifier
                                            .width(100.dp)
                                            .padding(end = 15.dp)
                                    )
                                }
                            }
                            Divider(thickness = 2.dp, color = Color(0xffd9d9d9))
                            Button(
                                onClick = { },
                                colors = ButtonDefaults.buttonColors(Color(0xfff9f9f9)),
                                modifier = Modifier
                                    .fillMaxWidth()
                                    .height(60.dp),
                                contentPadding = PaddingValues(0.dp),
                                shape = RoundedCornerShape(10.dp)
                            ) {
                                Row(
                                    horizontalArrangement = Arrangement.SpaceBetween,
                                    verticalAlignment = Alignment.CenterVertically
                                ) {
                                    Text(
                                        text = "컵&리드 옵션",
                                        fontSize = 20.sp,
                                        modifier = Modifier.width(300.dp),
                                        textAlign = TextAlign.Start,
                                        fontWeight = FontWeight.SemiBold,
                                        color = Color.Black
                                    )
                                    Text(
                                        text = "〉",
                                        color = Color(0xff767676),
                                        fontSize = 25.sp,
                                        textAlign = TextAlign.End,
                                        modifier = Modifier
                                            .width(100.dp)
                                            .padding(end = 15.dp)
                                    )
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}
```

-----------
강사님 코드
```kotlin
@Preview(showBackground = true)
@Composable
fun CoffeeList() {
    val coffeeList = listOf(
        Coffee(
            imageRes = R.drawable.ic_launcher_foreground,
            titleKo = "아이스 카페 아메리카노",
            titleEn = "Iced Caffe Americano",
            price = 4500,
            labelType = Coffee.LabelType.Best,
        ),
        Coffee(
            imageRes = R.drawable.ic_launcher_foreground,
            titleKo = "아이스 카페 라떼",
            titleEn = "Iced Caffe Latte",
            price = 5000,
            labelType = Coffee.LabelType.Best,
        ),
        Coffee(
            imageRes = R.drawable.ic_launcher_foreground,
            titleKo = "씨솔트 카라멜 콜드 브루",
            titleEn = "Sea Salt Caramel Cold Brew",
            price = 6300,
            labelType = Coffee.LabelType.New,
        ),
        Coffee(
            imageRes = R.drawable.ic_launcher_foreground,
            titleKo = "아이스 자몽 허니 블랙 티",
            titleEn = "Iced Grapefruit Honey Black Tea",
            price = 5700,
            labelType = Coffee.LabelType.Best,
        ),
        Coffee(
            imageRes = R.drawable.ic_launcher_foreground,
            titleKo = "콜드 브루",
            titleEn = "Cold Brew",
            price = 6900,
            labelType = Coffee.LabelType.New,
        )
    )
    Column(modifier = Modifier.verticalScroll(rememberScrollState())) {
        for (coffee in coffeeList) {
            CoffeeItem(coffee)
        }
    }
}
@Composable
fun CoffeeItem(coffee: Coffee) {
    Row(
        modifier = Modifier.padding(16.dp),
        verticalAlignment = Alignment.CenterVertically
    ) {
        Image(
            modifier = Modifier
                .size(64.dp)
                .clip(CircleShape),
            painter = painterResource(id = coffee.imageRes),
            contentDescription = "${coffee.titleEn} Image",
            contentScale = ContentScale.Crop,
        )
        Spacer(modifier = Modifier.size(16.dp))
        Column(
            verticalArrangement = Arrangement.spacedBy(8.dp)
        ) {
            Row(verticalAlignment = Alignment.CenterVertically) {
                Text(text = coffee.titleKo, fontWeight = FontWeight.Bold)
                Spacer(modifier = Modifier.size(8.dp))
                CoffeeLabel(labelType = coffee.labelType)
            }
            Text(text = coffee.titleEn, color = Color.Gray, fontWeight = FontWeight.Light)
            Text(text = coffee.displayPrice, fontWeight = FontWeight.Bold)
        }
    }
}
@Composable
fun CoffeeLabel(labelType: Coffee.LabelType) {
    when (labelType) {
        Coffee.LabelType.Best -> Text(
            text = "Best",
            fontStyle = FontStyle.Italic,
            fontSize = 8.sp,
            color = Color.Red
        )
        Coffee.LabelType.New -> Text(
            text = "New",
            fontStyle = FontStyle.Italic,
            fontSize = 8.sp,
            color = Color.Green
        )
        else -> Unit
    }
}
data class Coffee(
    val imageRes: Int,
    val titleKo: String,
    val titleEn: String,
    private val price: Int,
    val labelType: LabelType = LabelType.None,
) {
    val displayPrice: String
        get() {
            val dcFormat = DecimalFormat("#,###")
            return "${dcFormat.format(price)}원"
        }
    enum class LabelType {
        None, Best, New
    }
}
```

----------------------
